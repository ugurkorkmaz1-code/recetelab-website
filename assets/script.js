const langWrap=document.querySelector('.lang');document.querySelector('.lang>button')?.addEventListener('click',()=>langWrap.classList.toggle('open'));document.addEventListener('click',e=>{if(langWrap&&!langWrap.contains(e.target))langWrap.classList.remove('open')});
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));document.getElementById('year').textContent=new Date().getFullYear();
const header=document.querySelector('.header');addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>20),{passive:true});
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(!reduced){
 const glow=document.createElement('div');glow.className='cursor-glow';document.body.appendChild(glow);addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
 addEventListener('scroll',()=>document.querySelectorAll('.parallax').forEach((el,i)=>el.style.transform=`translate3d(0,${scrollY*(.025+(i%3)*.012)}px,0)`),{passive:true});
 document.querySelectorAll('.panel').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${-y*7}deg) rotateY(${x*9}deg) translateZ(8px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
 document.querySelectorAll('.magnetic,.btn').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.06}px,${(e.clientY-r.top-r.height/2)*.08}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
 const c=document.createElement('canvas');c.className='molecule-canvas';document.body.prepend(c);const x=c.getContext('2d');let pts=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(70,Math.floor(innerWidth/18))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.18,vy:(Math.random()-.5)*.14}))}resize();addEventListener('resize',resize);(function draw(){x.clearRect(0,0,innerWidth,innerHeight);for(let i=0;i<pts.length;i++){const p=pts[i];p.x=(p.x+p.vx+innerWidth)%innerWidth;p.y=(p.y+p.vy+innerHeight)%innerHeight;for(let j=i+1;j<pts.length;j++){const q=pts[j],d=Math.hypot(p.x-q.x,p.y-q.y);if(d<115){x.strokeStyle=`rgba(129,180,230,${(1-d/115)*.11})`;x.beginPath();x.moveTo(p.x,p.y);x.lineTo(q.x,q.y);x.stroke()}}x.fillStyle='rgba(255,130,35,.34)';x.beginPath();x.arc(p.x,p.y,1.4,0,7);x.fill()}requestAnimationFrame(draw)})();
}
document.getElementById('contactForm')?.addEventListener('submit',function(e){
 e.preventDefault();const d=new FormData(this),lang=document.documentElement.lang;
 const labels=lang==='ar'?{hello:'مرحباً ReçeteLab، أود الحصول على استشارة.',name:'الاسم',company:'الشركة',phone:'الهاتف',email:'البريد الإلكتروني',service:'الخدمة',message:'تفاصيل المشروع'}:lang==='tr'?{hello:'Merhaba ReçeteLab, danışmanlık almak istiyorum.',name:'Ad Soyad',company:'Firma',phone:'Telefon',email:'E-posta',service:'Hizmet',message:'Proje'}:{hello:'Hello ReçeteLab, I would like to request a consultation.',name:'Name',company:'Company',phone:'Phone',email:'Email',service:'Service',message:'Project'};
 const rows=[labels.hello,'',[labels.name,'name'],[labels.company,'company'],[labels.phone,'phone'],[labels.email,'email'],[labels.service,'service'],[labels.message,'message']].map(x=>Array.isArray(x)?`${x[0]}: ${d.get(x[1])||'-'}`:x);
 window.open(`https://wa.me/905537718776?text=${encodeURIComponent(rows.join('\n'))}`,'_blank','noopener');
});